{
    "configurations": [
        {
            "name": "Python: File",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "justMyCode": true,
            "cwd": "${fileDirname}",
            "purpose": ["debug-in-terminal"]
        }
    ]
}